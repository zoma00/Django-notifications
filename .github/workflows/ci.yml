name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install pytest==8.3.3 pytest-django==4.9.0 ruff

      - name: Lint (ruff)
        run: ruff check .

      - name: Django system checks
        run: python manage.py check

      - name: Test (pytest)
        run: pytest -v
